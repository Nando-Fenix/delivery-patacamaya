@extends('layouts.cliente')
@section('titulo','Mis direcciones')
@section('contenido-cliente')<div class="page-heading d-flex justify-content-between"><h1 class="h3">Mis direcciones</h1><a class="btn btn-primary" href="{{ route('cliente.direcciones.create') }}">Nueva</a></div><div class="d-grid gap-3">@forelse($direcciones as $d)<article class="card soft-card p-3"><div class="d-flex justify-content-between"><h2 class="h5">{{ $d->nombre }}</h2>@if($d->predeterminada)<span class="badge text-bg-success">Predeterminada</span>@endif</div><p>{{ $d->direccion_referencia }}</p><small>{{ $d->zona?->nombre }} · {{ $d->activo?'Activa':'Inactiva' }}</small><div class="d-flex gap-2 mt-3"><a class="btn btn-outline-primary" href="{{ route('cliente.direcciones.edit',$d) }}">Editar</a><form method="POST" action="{{ route('cliente.direcciones.estado',$d) }}">@csrf @method('PATCH')<button class="btn btn-outline-secondary">{{ $d->activo?'Desactivar':'Activar' }}</button></form>@if($d->activo&&!$d->predeterminada)<form method="POST" action="{{ route('cliente.direcciones.predeterminada',$d) }}">@csrf @method('PATCH')<button class="btn btn-outline-success">Predeterminada</button></form>@endif</div></article>@empty<div class="empty-state">Aún no guardaste direcciones.</div>@endforelse</div>@endsection