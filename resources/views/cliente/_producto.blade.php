<div class="col-12 col-sm-6 col-lg-4"><article class="card product-public-card h-100 {{ $producto->disponible?'':'unavailable' }}">@if($producto->imagen)<img src="{{ Storage::url($producto->imagen) }}" alt="Imagen de {{ $producto->nombre }}">@else<div class="product-public-placeholder"><i class="bi bi-image"></i></div>@endif<div class="card-body d-flex flex-column"><div class="d-flex justify-content-between gap-2"><h4 class="h6">{{ $producto->nombre }}</h4><strong class="text-nowrap">Bs {{ number_format((float)$producto->precio,2,',','.') }}</strong></div><p class="small text-secondary">{{ Str::limit($producto->descripcion,90) }}</p>@if($producto->disponible)<form class="mt-auto d-flex gap-2" method="POST" action="{{ route('cliente.carrito.store',$producto) }}">@csrf<input class="form-control quantity-input" type="number" name="cantidad" value="1" min="1" max="99" aria-label="Cantidad"><button class="btn btn-primary flex-grow-1"><i class="bi bi-cart-plus"></i> Agregar</button></form>@else<span class="badge text-bg-secondary align-self-start mt-auto">No disponible</span>@endif</div></article></div>