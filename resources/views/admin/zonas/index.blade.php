@extends('layouts.admin')
@section('titulo','Zonas — Delivery Patacamaya')
@section('contenido-admin')<div class="page-heading d-sm-flex justify-content-between"><div><h1 class="h2">Zonas</h1><p class="text-secondary">Cobertura administrativa interna.</p></div><a class="btn btn-primary" href="{{ route('administrador.zonas.create') }}">Nueva zona</a></div><form class="mb-3"><input class="form-control" name="buscar" value="{{ $buscar }}" placeholder="Buscar zona"></form>
@if($zonas->isEmpty())<div class="empty-state"><i class="bi bi-map fs-1"></i><h2 class="h5 mt-2">Aún no existen zonas de entrega</h2><a class="btn btn-primary mt-2" href="{{ route('administrador.zonas.create') }}">Crear primera zona</a></div>@else<div class="card soft-card overflow-hidden"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Zona</th><th>Relaciones</th><th>Estado</th><th></th></tr></thead><tbody>@foreach($zonas as $zona)<tr><td><strong>{{ $zona->nombre }}</strong><div class="small text-secondary">{{ $zona->descripcion }}</div></td><td>{{ $zona->negocios_count }} negocios · {{ $zona->direcciones_count }} direcciones</td><td>{{ $zona->activo?'Activa':'Inactiva' }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('administrador.zonas.edit',$zona) }}">Editar</a><form class="d-inline" method="POST" action="{{ route('administrador.zonas.estado',$zona) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">{{ $zona->activo?'Desactivar':'Activar' }}</button></form></td></tr>@endforeach</tbody></table></div></div>{{ $zonas->links() }}@endif
@endsection