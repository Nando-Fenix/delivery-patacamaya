@extends('layouts.app')
@section('contenido')
@php($unidadesCarrito = auth()->user()->carrito?->items()->sum('cantidad') ?? 0)
<div class="client-desktop-nav d-none d-md-flex mb-4"><a href="{{ route('cliente.inicio') }}">Inicio</a><a href="{{ route('cliente.buscar') }}">Buscar</a><a href="{{ route('cliente.carrito.index') }}">Carrito @if($unidadesCarrito)<span class="badge text-bg-success">{{ $unidadesCarrito }}</span>@endif</a><a href="{{ route('cliente.pedidos.index') }}">Pedidos</a><a href="{{ route('cliente.perfil') }}">Perfil</a></div>
@if(session('conflicto_carrito'))<div class="alert alert-warning"><strong>Tu carrito contiene productos de otro negocio.</strong><p>¿Deseas vaciarlo y comenzar un nuevo pedido?</p><form method="POST" action="{{ route('cliente.carrito.store',session('conflicto_carrito.producto')) }}">@csrf<input type="hidden" name="cantidad" value="{{ session('conflicto_carrito.cantidad') }}"><input type="hidden" name="reemplazar" value="1"><button class="btn btn-warning">Sí, vaciar y agregar</button></form></div>@endif
@yield('contenido-cliente')
<nav class="client-bottom-nav d-md-none"><a class="{{ request()->routeIs('cliente.inicio','cliente.buscar','cliente.negocios.*')?'active':'' }}" href="{{ route('cliente.buscar') }}"><i class="bi bi-search"></i><span>Explorar</span></a><a class="{{ request()->routeIs('cliente.carrito.*','cliente.checkout.*')?'active':'' }}" href="{{ route('cliente.carrito.index') }}"><i class="bi bi-cart"></i><span>Carrito @if($unidadesCarrito)({{ $unidadesCarrito }})@endif</span></a><a class="{{ request()->routeIs('cliente.pedidos.*')?'active':'' }}" href="{{ route('cliente.pedidos.index') }}"><i class="bi bi-receipt"></i><span>Pedidos</span></a><a class="{{ request()->routeIs('cliente.perfil','cliente.direcciones.*')?'active':'' }}" href="{{ route('cliente.perfil') }}"><i class="bi bi-person"></i><span>Perfil</span></a></nav>
@endsection